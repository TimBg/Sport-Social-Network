import FAQPage from './FAQPage';
import {connect} from 'react-redux';

const mapStateToProps = (state: any): Object => {
    return {};
}

const FAQContainer = connect(mapStateToProps, {})(FAQPage);

export default FAQContainer;