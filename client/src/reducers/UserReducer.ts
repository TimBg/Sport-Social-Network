let _ = require('lodash');

let initialState = {

};

let UserReducer = (state = initialState, action: any) => {
    let stateCopy;
        
    switch (action.type) {
        case 'SET_qwertt': 
        {
            
        }; return stateCopy;
        case 'ere': 
        {
            
        }; return stateCopy;
        default:
            break;
    }
    return state;
}

export default UserReducer;